import { ForCategoriesItemsDirective } from './for-categories-items.directive';

describe('ForCategoriesItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new ForCategoriesItemsDirective();
    expect(directive).toBeTruthy();
  });
});
